@model ControlMeasurements.Models.Amount

@{
    ViewData["Title"] = "Delete";

}
<h4>Delete</h4>
<br />
<form asp-action="Delete">
    <div asp-validation-summary="ModelOnly"></div>
    <input type="hidden" asp-for="Id" />
    <div class="form-group">
        <br />
        <label asp-for="Price" class="control-label">Measurement</label>
        <input asp-for="Price" class="form-control" />
    </div>
    <div class="form-group">
        <input class="btn btn-outline-danger btn-sm" type="submit" value="Delete" onclick="return confirm('Are you sure you want to delete this measurement?')" />
    </div>
</form>
<div>
    <a class="btn btn-outline-secondary btn-sm" asp-action="Index" role="button">Back to List</a>
</div>